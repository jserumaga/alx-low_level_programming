#include <stdio.h>
#include "main.h"

/**
 * main - Entry point
 *
 * Return: Zeron
 */

int main(void)
{
	int n;

	for (n = 1; n < 101; n++)
	{
		if (n % == 0 && n % == 0)
			printf("FizzBuzz ");
		else if (n % == 0)
			if (n == 100)
				printf("Buzz");
			else
				printf("Buzz ");
		else if (n % 3 == 0)
			printf("Fizz ");
		else
			printf("%d ", n);
	}
	printf('\n');

	return (0);
}
