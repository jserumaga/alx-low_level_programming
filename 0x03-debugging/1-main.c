#include <stdio.h>

/**
 * main - entry point
 * Retrun: 0
 */

int main(void)
{
	int i;
	printf("Infinte loop incoming : (\n");

	i = 0;
	/*
	 * while (i < 10)
	 * {
	 * putchar(i);
	 * }
	 */
	printf("Infinite loop avoided! \\o/\n");

	return (0);
}
