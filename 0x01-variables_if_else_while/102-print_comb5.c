#include <stdio.h>
/**
 * main - Entry point
 *
 * Description
 *
 * Return: Zero when successful
 *
 */
int main(void)
{
	int x = 0, y = 0, z = 0;

	for (x = 1; x <= 99; x++)
	{
		for (y = 1; y <= 99; y++)
		{
			for (z = 1; z <= 99; z++)
			
			{
				if(x != y && y !- z && z != x)
				{
					putchar("%d %d %d\n", x, y, z)
				
				}
			
			}
		
		}
	
	}

}
