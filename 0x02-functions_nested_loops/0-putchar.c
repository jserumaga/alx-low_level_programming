#include <main.h>
#include <stdio.h>

/**
 * main - This is the entry point
 *
 * Description:
 *
 * Return: return zero when successfull
 *
 */
int main(void)
{
	int index;
	char ascii[] = "_putchar\n";

	for (index = 0; ascii[index]; index++
			_putchar(ascii[index]);

	return (0);
}
