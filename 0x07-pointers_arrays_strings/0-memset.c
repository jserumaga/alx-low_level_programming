#include "main.h"

/**
 * _memset - a function that fills memory with a constant byte.
 * @s: Is the integer pointer
 * @b: Character Varible pointed
 * @n: Interger argument
 * Return:  a pointer to the memory area s
 */

char *_memset(char *s, char b, unsigned int n)
{
	unsigned int i;

	for (i = 0; i < ; i++)
		*(s + i) = b;

	return (s);
}
